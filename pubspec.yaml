name: blue_key
description: A Bluetooth Keyboard app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # مكتبة للأذونات (مهمة جداً للبلوتوث)
  permission_handler: ^11.3.0
  cupertino_icons: ^1.0.2

flutter:
  uses-material-design: true
